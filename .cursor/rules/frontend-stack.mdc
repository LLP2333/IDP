---
description: 前端 T3 Stack 编码规范（Next.js + TS + tRPC + Drizzle）
globs: frontend/**/*.{ts,tsx,js,jsx,mjs,cjs}
alwaysApply: false
---

# 前端编码规范

## 技术栈基线

- **Next.js 15 App Router**（`src/app/`）；不要使用 Pages Router。
- **React 19** 函数式组件 + Hooks；禁止 class component。
- **TypeScript strict** 已开启，禁止使用 `any`，确实需要时用 `unknown` 并收窄。
- **Tailwind CSS v4** + `prettier-plugin-tailwindcss`（class 顺序由 Prettier 自动整理）。
- **tRPC v11** + TanStack React Query。
- **NextAuth v5** beta。
- **Drizzle ORM** + PostgreSQL（`drizzle-orm/node-postgres`）。

## 模块组织

```text
src/
├── app/            # 路由 + 页面 + Route Handlers
│   └── _components # 仅当前路由复用的组件
├── lib/            # 通用纯函数工具
├── server/         # 服务端代码，文件首行加 `import "server-only"` 或仅服务端导入
│   ├── api/        # tRPC routers
│   ├── auth/       # NextAuth 配置
│   └── db/         # Drizzle schema + 客户端
├── trpc/           # tRPC 客户端封装
├── styles/
└── env.js          # 环境变量校验入口
```

- **路径别名**：使用 `~/xxx` 引用 `src/xxx`，禁止深层相对路径。
- **服务端代码**：放在 `src/server/`，跨模块只导入 `*.ts` 不导入服务端依赖。
- **环境变量**：必须先在 `src/env.js` 中声明并通过 zod 校验，组件内通过 `env.XXX` 访问，禁止直接读 `process.env`。

## 测试要求

- 使用 **Vitest**（已配置 jsdom + Testing Library）。
- 测试文件与代码同目录，命名 `*.test.ts` / `*.test.tsx`。
- 新增 / 修改任何公共工具、组件、tRPC procedure，都必须附带测试。
- 提交前 `pnpm test` 必须全绿。

## 常见反例

```ts
// ❌ 禁止：直接读 process.env
const url = process.env.NEXT_PUBLIC_API_BASE_URL;

// ✅ 正确：通过校验后的 env 对象
import { env } from "~/env";
const url = env.NEXT_PUBLIC_API_BASE_URL;
```

```tsx
// ❌ 禁止：在客户端组件中导入 server-only 模块
"use client";
import { db } from "~/server/db";

// ✅ 正确：客户端通过 tRPC 调用
"use client";
import { api } from "~/trpc/react";
const { data } = api.post.getLatest.useQuery();
```
