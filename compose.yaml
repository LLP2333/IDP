services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=idp'
      - 'POSTGRES_PASSWORD=idp'
      - 'POSTGRES_USER=idp'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
